modules:
  - name: runtime
    config:
      "@type": cosmos.app.runtime.v1alpha1.Module
      app_name: SimApp
      begin_blockers: [ staking ]
      end_blockers: [ staking ]
      init_genesis: [ auth, bank, staking, genutil, hyperlane, warp, nova ]
      override_store_keys:
        - module_name: auth
          kv_store_key: acc
  - name: auth
    config:
      "@type": cosmos.auth.module.v1.Module
      bech32_prefix: noble
      module_account_permissions:
        - account: fee_collector
        - account: bonded_tokens_pool
          permissions: [ burner, staking ]
        - account: not_bonded_tokens_pool
          permissions: [ burner, staking ]
        - account: hyperlane
        - account: warp
          permissions: [ burner, minter ]
  - name: bank
    config:
      "@type": cosmos.bank.module.v1.Module
      blocked_module_accounts_override:
        [ auth, bonded_tokens_pool, not_bonded_tokens_pool ]
  - name: consensus
    config:
      "@type": cosmos.consensus.module.v1.Module
  - name: genutil
    config:
      "@type": cosmos.genutil.module.v1.Module
  - name: staking
    config:
      "@type": cosmos.staking.module.v1.Module
  - name: tx
    config:
      "@type": cosmos.tx.config.v1.Config

  - name: hyperlane
    config:
      "@type": hyperlane.core.module.v1.Module
      authority: noble1kg0mtdjvaqdnk5hw599na9mq66vs2c8zanes8t
  - name: warp
    config:
      "@type": hyperlane.warp.module.v1.Module
      authority: noble1kg0mtdjvaqdnk5hw599na9mq66vs2c8zanes8t
      enabled_tokens:
        - 1 # Enable Collateral tokens
        - 2 # Enable Synthetic tokens

  - name: nova
    config:
      "@type": nova.module.v1.Module
      # NOTE: This is a fake account used for local testing.
      # deny enhance title bind tunnel drill zebra daring hurt hedgehog outer suspect please suffer cinnamon able relief hen collect female capital jaguar page stand
      authority: noble1kg0mtdjvaqdnk5hw599na9mq66vs2c8zanes8t
